services:
  - type: web
    name: pause-emotional-guide
    env: node
    buildCommand: "npm install"
    startCommand: "node server.js"
    publishDir: "."  # ← ADD THIS LINE!
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /
